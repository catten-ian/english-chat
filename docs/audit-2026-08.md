# ai-english-chat 代码审查与修复记录（2026-08）

审查范围：`ai-english-chat/` 应用本身（`server.js`、`index.html`、`css/`、`js/`、`README.md`、`WIKI.md`）。
不含主应用 `article-memorizing/`。

审查方式：全程只读分析 + 修复后在**临时副本**（`D:\temp\opencode`，不含 `.env`/真实数据库）上启动服务做 HTTP 回归。

---

## 目录

- [一、总体评价](#一总体评价)
- [二、已修复问题](#二已修复问题)
  - [P0 安全](#p0-安全)
  - [P1 数据可靠性](#p1-数据可靠性)
  - [P1 任务生命周期](#p1-任务生命周期)
  - [P2 功能正确性](#p2-功能正确性)
  - [文档同步](#文档同步)
- [三、按用户要求跳过的项](#三按用户要求跳过的项)
- [四、验证结果](#四验证结果)
- [五、未修复的遗留项](#五未修复的遗留项)
- [六、体验改善建议](#六体验改善建议)
- [七、新功能建议](#七新功能建议)
- [八、长期拓展方向](#八长期拓展方向)
- [九、五批执行路线](#九五批执行路线)

---

## 一、总体评价

### 原有的良好基础

- 后端仅绑定 `127.0.0.1`，局域网不可达
- 密钥只在服务端 `.env`，前端 `js/config.js` 不含 key
- SQLite 启用 WAL + 外键
- SQL 值均参数化，未发现 SQL 注入
- session token 用 `crypto.randomBytes(32)` 生成
- 密码 PBKDF2 加盐 + `timingSafeEqual` 比较
- `user_data` 查询均带 `user_id`
- 备份使用 `VACUUM INTO`（比运行中直接复制 `app.db` 可靠）
- 产品闭环清晰：Chat / Reading / Writing / Translation / Anki / 多 Agent

### 本轮发现的主要问题

1. 静态目录穿越可读取 `.env`、`data/app.db`、备份文件（未鉴权）
2. 模型输出的 HTML 可在页面执行（两处 DOM XSS）
3. 数据库层按 `user_id` 隔离，但浏览器 localStorage 未按账户隔离
4. 保存为异步全量覆盖，存在旧快照覆盖新数据的竞争
5. 「停止」不能中止上游与后台分析，且会留下空消息节点
6. 文档宣称的「修改密码」后端没有实现
7. `app.js` 已 7900+ 行，多领域状态互相污染
8. 无任何自动化测试

---

## 二、已修复问题

### P0 安全

#### 1. 静态目录穿越（未鉴权读取 `.env` / 数据库 / 备份）

**位置**：`server.js` 原 `serveStatic()`（原 317-331 行）

**原实现的缺陷顺序**：

```js
if (!/^\/(index\.html|css\/|js\/|vendor\/|music\/)/.test(pathname)) { 404 }  // 先用原始 pathname 匹配前缀
decoded = decodeURIComponent(pathname);                                       // 再解码
const abs = path.normalize(path.join(BASE, decoded));                         // 再归一化
if (!abs.startsWith(BASE)) { 404 }                                            // 只保证没离开 BASE 整体
```

三个问题叠加：

- 前缀检查在解码之前，`%2e%2e` 类编码可绕过
- `startsWith(BASE)` 只保证「没离开应用根目录」，不保证「在 `js/`、`css/` 等公开目录内」
- 扩展名不在白名单时兜底 `application/octet-stream`，因此 `.env`、`.db` 仍会被发送

**可利用路径**：

```
/js/../.env
/css/../data/app.db
/vendor/../data/backups/<ts>_chat.db
/js/%2e%2e/.env
```

**影响**：泄露 MiniMax / ElevenLabs key、含密码哈希与明文 bearer token 的数据库、全部用户数据与备份。服务仅绑回环降低了远程利用面，但本机恶意页面 / 扩展 / DNS rebinding 仍可利用；一旦反代暴露即变为远程可利用。

**修复**：改为「URL 前缀 → 独立物理根目录」显式映射。

```js
const STATIC_DIRS = {
  '/css/':    path.join(BASE, 'css'),
  '/js/':     path.join(BASE, 'js'),
  '/vendor/': path.join(BASE, 'vendor'),
  '/music/':  MUSIC_DIR
};
```

新增 `resolveInside(root, relPath)`，逐项校验：

- 先 `decodeURIComponent`，失败返回 400
- 拒绝 `\0`、反斜杠 `\`
- 逐段拒绝空段、`.`、`..`
- 拒绝绝对路径
- `path.resolve` 后要求 `resolved === rootResolved || resolved.startsWith(rootResolved + path.sep)`

新增 `sendFile()`：扩展名必须在 `STATIC_MIME` 中，**去掉 octet-stream 兜底**，并统一加 `X-Content-Type-Options: nosniff`。

#### 2. Markdown 代码块 DOM XSS

**位置**：`js/app.js` `renderMD()`（代码块提取 59-64 行、恢复处原 140 行）

fenced code 内容先被抽出（verbatim），整体 HTML 转义后再恢复，但恢复时未转义：

```js
// 修复前（注释写着 escaped，实际没有）
s.replace(/\u0000CODE(\d+)\u0000/g, (m, i) => '<pre class="code-block">' + codeBlocks[Number(i)] + '</pre>');
```

模型只要回复:

````
```html
</pre><img src=x onerror="...">
```
````

即可注入并由 `addAiMessage()` 的 `innerHTML` 执行。

**影响**：应用同源脚本执行 → 读取 localStorage 中的长期 bearer token → 以用户身份调用所有本地 API。

**修复**：

```js
s.replace(/\u0000CODE(\d+)\u0000/g, (m, i) =>
  '<pre class="code-block"><code>' + esc(codeBlocks[Number(i)]) + '</code></pre>');
```

#### 3. Cloze 题目 DOM XSS

**位置**：`js/app.js` `clRenderQuestions()`（原 7275 行）

`q.sentence` 直接来自 LLM，未转义就 `replace('____', ...)` 后拼进 `wrap.innerHTML`。

**修复**：按 `____` 切分，前后文各自 `esc()`，只保留自己生成的空格标签。

```js
const cut = raw.indexOf('____');
const sentenceHtml = cut >= 0
  ? esc(raw.slice(0, cut)) + '</span>' + blank + '<span>' + esc(raw.slice(cut + 4))
  : esc(raw);
```

#### 4. 请求体上限可被 chunked 绕过

**位置**：`server.js` `readBody()`（原 299-307 行）

只检查 `Content-Length`；不带该头或使用 `Transfer-Encoding: chunked` 时 `len` 为 0，随后无上限累积 `chunks`。

**修复**：`readBody(req, limit)` 在每个 `data` 事件累计真实字节；超限立即 `resolve(null)`（handler 回 413），清空已收集 chunk，保留 `data` listener 消耗剩余流，Promise 只 settle 一次。同时为不同接口设独立上限：

- `/api/db/*`：`MAX_USER_DATA` = 8MB
- `/api/auth/change-password`：8KB
- 其余：`MAX_BODY` = 24MB

并补上 `/api/auth/login` 缺失的 `!body` 检查（原先 body 为 null 时 `body.toString()` 会抛错）。

---

### P1 数据可靠性

#### 5. 跨账户 localStorage 串号

**位置**：`js/storage.js` `loadUserData()`（原 138-154 行）、`js/app.js` `logoutUser()`（原 4569-4582 行）

原逻辑只在服务端返回非空时才覆盖本地缓存：

```js
if (convs && typeof convs === 'object') localStorage.setItem('ai_en_convs', ...);
```

而业务读取用的是固定 key（`ai_en_convs` / `ai_en_vocab` / `ai_en_weak` / `ai_en_setting_*`），登出又只清 4 个 key。

**后果**：A 用户产生缓存 → 异常退出或会话过期 → B 用户登录且服务端某些 key 为空 → B 看到 A 的对话/设置/阅读历史，且后续保存会把 A 的数据写进 B 的数据库行。

**修复**（选择 owner 标记方案，避免现有 catten 缓存因 key 重命名而失效）：

`js/storage.js` 新增：

- `CACHE_OWNER_KEY = 'ai_en_cache_owner'`
- `USER_CACHE_KEYS`：`ai_en_convs`/`vocab`/`weak`/`current_conv`/`settings_backup`/`backup_latest`/`backup_history`/`dict_history`/`mode`/`game_tab`
- `clearUserCache()`：清上述 key + 遍历清除所有 `ai_en_setting_*`
- `ensureCacheOwner(username)`：owner 与当前用户不一致 → 清空；owner 缺失（老版本升级）→ 视为当前用户，仅补写标记

调用点：

- `loadUserData()` 开头
- `doLogin()` 成功后（`js/app.js`）
- `tryBoot()` 中 `apiMe()` 返回后，以服务端用户名为权威值
- `logoutUser()` 改为调用 `clearUserCache()` 并移除 owner 标记

另外 `loadUserData()` 现在在服务端返回 `null` 时**显式写入空默认值**（`{}` / `[]` / `''`），不再保留旧值。

#### 6. 保存乱序导致数据回退

**位置**：`js/storage.js` `apiSave()`（原 81-92 行）及所有 `save*` 调用方

`apiSave` 是 fire-and-forget，多个全量快照可同时在途。若「用户消息」的请求晚于「含 AI 回复+反馈」的请求到达服务端，服务端最终保存的是缺内容的旧快照。

**修复**：per-key 串行队列 + 快照合并。

```js
const _saveInflight = {};   // key -> Promise
const _savePending = {};    // key -> 最新待发数据（只保留一份）
const syncStatus = { pending, failed, lastError, lastSavedAt };
```

同 key 有请求在途时，新数据覆盖 `_savePending[key]` 并复用同一队列；在途请求结束后循环消费 pending，只发送最后一份。同时区分「HTTP 非 2xx」与「网络异常」并记入 `syncStatus`（为后续「同步状态」UI 预留）。

#### 7. `/api/db/*` 无结构校验，损坏值可覆盖有效数据

**位置**：`server.js` user_data 路由（原 425-431 行）

原实现直接把请求体文本写入 `user_data.value`，GET 时 `JSON.parse` 失败静默返回 `null`——表现为「数据凭空消失」。

**修复**：

```js
const USER_DATA_KEYS = {
  conversations: 'object', vocab: 'array', weak: 'object', settings: 'object',
  reading: 'object', characters: 'array', avatar: 'string', strategist: 'array'
};
```

- POST：先 `JSON.parse`（失败 400 `invalid json`），再用 `matchesType()` 校验顶层类型（不符 400 `invalid shape`），限制 8MB
- GET：解析失败时打印 `[DB] user <id> key <key>: 存储值不是合法 JSON` 后再返回 null，不再静默

#### 8. 修改密码接口缺失

**位置**：前端 `js/storage.js:69-77` 调用 `POST /api/auth/change-password`；`server.js` 无此路由；`WIKI.md:67-73` 宣称可用

**修复**：新增该路由：

- 校验旧密码（`verifyPassword`）
- 新密码长度 4–200
- 事务内更新 `password_hash` + `DELETE FROM sessions WHERE user_id=? AND token != ?`
- 返回 `{ status:'ok', revoked_sessions: n }`，当前会话保留
- 请求体上限 8KB

#### 9. 设置未按账户恢复 / 陈旧快照覆盖

**位置**：`js/storage.js` 原 146-147、312-319 行；`js/app.js` `getSetting/setSetting`（2163-2168 行）、`saveSettings()`

两个问题：

1. `loadUserData()` 把服务端 settings 只写进 `ai_en_settings_backup`，而实际读取用的是 `ai_en_setting_*` → 服务端设置无法恢复
2. `saveSettings()` 未同步更新 backup 快照 → 之后 `setCurrentConvId()` 读到旧 backup，只改 `current_conv` 就整体 POST，把刚保存的新设置覆盖回旧值

**修复**：

- 新增 `hydrateSettingsFromServer(settingsObj)`：展开写入真实 `ai_en_setting_*`；`current_conv` 单独写 `ai_en_current_conv`，不当作偏好项
- 新增 `saveSettingsBackup(settings)`
- `setCurrentConvId()` 在 POST 前先 `saveSettingsBackup()`
- `saveSettings()` 把合并后的对象存入 `mergedSettings`，先 `saveSettingsBackup(mergedSettings)` 再 `apiSave`

#### 10. 新账户首次启动结构错误

**位置**：`js/app.js` 原 7675-7679 行

```js
const conv = { id, title, topic, ..., messages: [] };
conversation = conv;          // 但 conversation 全局被当作「版本树数组」使用
setCurrentConvId(newId);
saveConversation(conv);       // convs[id] 不存在 → 函数开头 return，静默不保存
```

`getActivePath()` 内 `for (const n of nodes)` 对普通对象会抛 `not iterable`。

**修复**：

```js
conversation = [];
createConversation('新对话', 'free');   // 由统一入口写入 convs map 并设 current id
```

---

### P1 任务生命周期

#### 11. SSE 停止不贯穿上游

**位置**：`server.js` `/api/proxy/chat/stream`（原 738-764 行）

- 60 秒 timer 在收到响应头后即 `clearTimeout` → 只覆盖「等首字节」，不覆盖整个流
- 未监听 `req.aborted` / `req.close` / `res.close` → 用户点停止后服务端仍继续从 MiniMax 读取并计费
- 上游非 2xx 也照样 `writeHead(200, 'text/event-stream')` → 前端只看到空回复，无法区分 401/429/400
- 无背压处理

**修复**：

- 单一 `AbortController` 贯穿全程，`abortUpstream()` 绑定 `req.aborted` / `req.close` / `res.close`
- 三层超时：`PROXY_TIMEOUT`(60s，建连/首字节) + `STREAM_IDLE_TIMEOUT`(90s，每收到数据重置) + `STREAM_TOTAL_TIMEOUT`(10min)
- `!upstream.ok || !upstream.body` 时读取有限错误体（≤500 字符），透传真实状态码
- `res.write()` 返回 false 时等 `drain`
- `finally` 中 `reader.cancel()` + 清理全部 timer 与 listener
- 客户端主动断开不记为错误日志

#### 12. 停止后残留幽灵消息

**位置**：`js/app.js` `sendMessage()`（空节点 2456-2459 行、abort 路径原 2475-2479 行）

空 assistant 节点在请求开始前就 `appendToEnd`，abort 时只调 `removeTyping()`，节点仍留在内存树中；下一次保存即被持久化，刷新后出现空气泡 / `(no response)`，且后续用户消息挂在空节点之后。

**修复**：将 `aiNode` / `aiMsgId` 提到 try 外（`aiNodeRef` / `aiMsgIdRef`），新增：

```js
function finalizeAbortedReply(aiNode, aiMsgId, isError) {
  // 有部分文本 → 写入节点并标记 v.cancelled
  // 无内容      → removeNodeFromTree(aiNode.id)
  // 之后 renderMessages() + saveConversation()
}
```

排除占位文本 `🔎 执行者研究中...`（不算真实回复）。abort 与 error 两条路径都调用。

#### 13. 异步评分污染其他对话

**位置**：`js/app.js` `callAnalysis()`（2239 行起）、`sendMessage()` finally（`currentAbort = null`）

`callAnalysis` 未 await，主回复一结束 `currentAbort` 即被清空，分析仍在后台跑。此时用户切换对话，旧分析完成后仍会 `trackWeakPoints()`、推 Anki、并 `renderFeedbackForMsg()` 清空**当前**对话的反馈区。retry 的 `setTimeout` 也无人管理。

**修复**：

- 新增全局 `analysisAbort`（独立 controller）、`analysisTimers[]`
- `cancelAnalysisTasks()`：abort + 清空所有重试 timer
- `analysisStillValid(ctx)`：校验 `currentUser()`、`getCurrentConvId()`、`findNode(msgId)` 三者均未变
- `callAnalysis(userText, msgId, attempt, signal, ctx)` 首次调用时捕获 `{ convId, msgId, user }`，重试沿用
- 结果返回后先 `analysisStillValid()` 才写任何状态（否则 `dbg('ANALYSIS_STALE')` 丢弃）
- `scheduleRetry()` 统一登记 timer 并在触发时再次校验
- 调用 `cancelAnalysisTasks()` 的时机：`stopSending()`、`resumeConversation()`、`startNewConversation()`、`logoutUser()`

---

### P2 功能正确性

#### 14. 阅读划词引用未定义变量

**位置**：`js/app.js` `onReadingSelection()`（原 5703 行）

```js
if (!content.contains(range.commonAncestorContainer)) return;   // content 未声明
```

依赖浏览器把元素 id 暴露为全局变量，某些环境下抛 `ReferenceError`/`TypeError`，且错误在延迟回调中，用户只看到「没反应」。

**修复**：函数内 `const content = document.getElementById('rdContent');` 并判空。

#### 15. 阅读高亮无法恢复

**位置**：`js/app.js` `addHighlight()`、`applyHighlights()`（原 6025-6028 行）

高亮只存 `{id, text, color, note, createdAt}`，无位置信息；`applyHighlights()` 是空实现（只更新计数）。刷新、切回历史、重新打开文章后正文中没有任何高亮。

**修复**：

- 新增 `hlPosition(range)`：用 `range.cloneRange()` + `setStart(pEl, 0)` 计算 `{ pIdx, startOffset, endOffset }`
- `addHighlight()` **在 `deleteContents()` 之前**计算位置（之后 range 会收缩到插入点）
- `applyHighlights()` 重写：按 `pIdx` 分组、组内按 `endOffset` 降序（从后往前插，避免偏移漂移），用 `pText.slice(start,end) === h.text` 双重校验后重建 span；单条失败不影响其他

#### 16. 阅读 TTS Blob 泄漏 + 叠加播放

**位置**：`js/app.js` `readingTts()`（原 6155-6156 行）

```js
new Audio(URL.createObjectURL(blob)).play().catch(() => {});
```

无引用、不 revoke、无法停止，重复点击会叠加播放。

**修复**：新增模块级 `readingAudio` + `stopReadingTts()`；播放前先停上一个，`ended`/`error` 时 `revokeObjectURL`，`play()` 失败也释放并提示。

#### 17. 写作「开始写作」抛异常

**位置**：`js/app.js` `startWriting()`（原 6114 行）

```js
document.getElementById('wFeedback').style.display = 'none';   // 该节点已不存在
```

反馈早已迁到右侧统一面板，此行必然抛错，导致 `updateWordCount()` 不执行。

**修复**：删除该行。同时修正 `hasUnsavedDraft()`（原 5496/5501 行）——原本用不存在的 `#wFeedback`/`#trFeedback` 判断是否已提交，改为检查 `#wAnnOverlay`/`#trAnnOverlay` 是否含 `visible` class。

#### 18. 题库 `source_file` 恒为空

**位置**：`server.js` 单卷查询（原 608-610 行）

SELECT 未包含 `source_file`，但响应中读 `rows[0].source_file`。

**修复**：SELECT 加上 `source_file`。

#### 19. Anki 推送误标已推 + 缺少入参校验

**位置**：`server.js` `/api/gaokao/push-to-anki`（原 649、694-708 行）

- 无论 `addNotes` 是否为该题返回有效 noteId，都把**所有**请求题目记入 `gaokao_pushed`
- 未检查 AnkiConnect 顶层 `error`（会被当作 `added=0` 的 200 返回）
- `parseInt("1junk")` 被接受为 1；无批量上限；无去重
- 注释声称「先 canAddNotes 去重」，实际没有调用

**修复**：

- `ids`：批量上限 100（超出 400），`Number.isSafeInteger(x) && x > 0` 过滤，`Set` 去重
- JSON 解析失败 → 502 `anki bad response`
- `result.error` 非空 → 502 `anki error`
- 逐题结果 `perQuestion = rows.map((r,i) => ({ id: r.id, added: Number.isInteger(noteIds[i]) }))`
- **只把 `added === true` 的题写入 `gaokao_pushed`**
- 响应新增 `questions` 逐题明细；保存失败改为 `console.error` 而非静默
- 删除与实现不符的 canAddNotes 注释

#### 20. SSE 解析丢尾 + 流式渲染卡顿

**位置**：`js/app.js` `streamChat()`（原 442-459 行）、`streamDict()`（原 524-547 行）、`appendStreamDelta()`（原 562-565 行）

- 循环结束后未 `decoder.decode()` flush，也未处理残余 `buffer` → 末尾 token 或完整末事件可能丢失
- 每个 delta 都 `el.textContent += delta`（全量重建，O(n²) 倾向）并 `scrollToBottom()`（多次布局读取）

**修复**：

- 两个 stream 函数抽出 `handleLine()`，EOF 后 `buffer += decoder.decode()` 并处理最后一行；`streamChat` 另抽 `consumeSSE(payload)`
- `appendStreamDelta()` 改为节流：delta 进 `_streamThrottle.buf`，`requestAnimationFrame` 每帧批量写入一次，优先用 `firstText.appendData(text)` 追加到已有文本节点，每帧只滚动一次

#### 21. 备份同秒名冲突

**位置**：`server.js` `/api/backup`（原 727-735 行）

文件名精度只到秒，`VACUUM INTO` 要求目标不存在 → 同秒第二次备份直接 500（实测复现：`output file already exists`）。

**修复**：

- 新增 `_backupInFlight` 互斥锁，并发时返回 409
- 目标已存在时回退名 `<ts>_<ms后6位><random>_chat.db`
- `pruneBackups()` 正则改为 `^\d{14}(?:_\d+)?_chat\.db$` 以兼容回退名
- prune 失败改为 `console.error`，不影响本次备份响应

#### 22. 题库重建先删后解析

**位置**：`server.js` `seedGaokao()`（原 172-179 行）

```js
db.exec('DELETE FROM gaokao_questions');   // 在 BEGIN 之前
const data = JSON.parse(fs.readFileSync(fp, 'utf8'));   // 这里若失败，旧题库已永久丢失
db.exec('BEGIN');
```

**修复**：调整顺序——先 `readFileSync` + `JSON.parse` + `Array.isArray` 校验，再进入单个事务，`DELETE` 与 `INSERT` 都在事务内。失败时日志明确标注「旧数据未受影响」。同时统计实际导入的试卷数/题数（而非直接用 `data.length`）。

#### 23. 登录框 input 属性引号损坏

**位置**：`index.html:813`

```html
<input id="loginUser" type="text" placeholder="用户名 autocomplete="username" aria-label="用户名>
```

`placeholder`、`autocomplete`、`aria-label` 解析结果依赖浏览器容错，自动填充与屏幕阅读器标签不可靠。

**修复**：补全引号。

#### 24. 健康检查显示 undefined + 过时提示

**位置**：`js/app.js` 原 4522-4526 行；`server.js` `/api/health`

前端读不存在的 `d.data_dir` → 显示「在线 (undefined)」；离线提示写「需运行 server.py」（该文件已不存在）。`/api/health` 还返回数据库绝对路径。

**修复**：

- 前端改为显示 `✅ 在线 · MiniMax ✓/✗`，离线提示改为「需运行 node server.js」
- `/api/health` 移除 `db: DB_PATH`
- 同时修正 `js/app.js:4621` 附近注释中残留的 `server.py`

---

### 文档同步

`README.md`：

- 「4 大功能模块」→ 6 大（Chat/Reading/Practice/Writing/Translation/Game，Game 含 Charade/Cloze/Wordle）
- KaTeX「CDN」→ 本地 vendored `vendor/katex`
- `app.js` 行数 4100+ → 7600+
- 澄清 `scripts/manage_users.py` 位于工作区根目录（`ai-english-chat` 的上一级）
- 安全小节补充：修改密码撤销其他会话、`/api/db/*` 强校验、本地缓存账户归属
- 增加「首次使用请尽快修改默认密码」提示

`WIKI.md`：

- token 存储说明：默认勾选「记住我」→ localStorage 30 天；取消 → sessionStorage 关标签即退出；公共电脑勿勾选
- 修改密码：补充长度上限与后端接口名
- 数据备份：明确由**浏览器页面**每 2 分钟触发（页面未打开不会备份），补充同秒回退命名与分层保留说明
- 常见问题新增 6 条（换账户串号、停止不生效、幽灵空气泡、高亮丢失、数据回退）

---

## 三、按用户要求跳过的项

以下问题已确认存在，用户明确要求本轮不修：

### S-1 Anki 卡片 HTML 未净化（DOM XSS 路径）

**位置**：`js/app.js:2081-2089`（`showWebReviewQuestion`）、`2101-2111`（`webReviewShowAnswer`）

`cardData.question` / `cardData.answer` / `fields.Question` 原样插入 `modal.innerHTML`。Anki 卡片允许 HTML，若卡片来自第三方牌组或被污染模板，事件属性 / SVG / iframe 可能形成执行路径。

**用户决定**：自己的 Anki 自行管理，不会有恶意卡片。

**如需修复**：优先 `textContent` 显示纯文本；需要格式时用标签白名单（保留 `b/strong/em/br/div/span/ul/ol/li`，剥离所有 `on*`、`style`、`iframe`、`object`、`embed`、SVG），或放入无脚本 sandboxed iframe。

### S-2 默认弱口令账户

**位置**：`server.js:211-215`（播种 `test/test`、`catten/catten`）、启动日志、`index.html:829`、README/WIKI

`catten` 是承接旧 JSON 数据迁移的真实账户，密码与用户名相同。

**用户决定**：当前为本地测试，暂不修改。

**如需修复**：改为首次启动生成一次性随机密码 / CLI 创建首个用户；测试账户仅在 `SEED_DEMO_USERS=1` 时播种；首次登录强制改密；移除日志与文档中的明文凭据。

> 注：`/api/auth/change-password` 已实现，可随时在设置面板改掉默认密码。

### S-3 登录限流

**位置**：`server.js` `/api/auth/login`；`hashPassword`/`verifyPassword` 使用同步 `pbkdf2Sync`

无频率限制、失败计数、退避或锁定。每次失败登录的 10 万次 PBKDF2 在主线程执行，并发爆破同时构成 CPU DoS（阻塞事件循环）。

**用户决定**：暂不需要。

**如需修复**：按 IP / 用户名 / 组合三层限流 + 指数退避；PBKDF2 改异步或移入 worker。

---

## 四、验证结果

`node --check` 全部通过：`server.js`、`js/app.js`、`js/storage.js`、`js/config.js`、`js/shgaoka_bank.js`。

HTTP 回归在临时副本上执行（`D:\temp\opencode\ai-english-chat-test`，端口 8092，**不含 `.env` 与真实数据库**），测试后已删除副本。

### 静态路径穿越

| 请求路径 | 结果 |
|---|---|
| `/js/../.env` | 404 |
| `/css/../data/app.db` | 404 |
| `/vendor/../data/app.db` | 404 |
| `/js/%2e%2e/.env` | 404 |
| `/js/..%2f.env` | 404 |
| `/js/%2e%2e%2f.env` | 404 |
| `/music/../.env` | 404 |
| `/js/%00.env` | 400 |
| `/js/..\.env` | 404 |
| `/js/%2e%2e%5c.env` | 404 |
| `/js/....//.env` | 404 |

正常资源全部 200：`/index.html`、`/js/app.js`、`/js/storage.js`、`/js/config.js`、`/js/shgaoka_bank.js`、`/css/style.css`、`/vendor/katex/katex.min.js`、`/vendor/katex/fonts/KaTeX_Main-Regular.woff2`，以及 URL 编码的中文音乐文件名。

### 认证与修改密码

```
[1] login test/test              -> 200
[2] change-pw 错误旧密码          -> 401 ✓
[3] change-pw 新密码过短          -> 400 ✓
[4] change-pw 正确                -> 200 {"status":"ok","revoked_sessions":1} ✓
[5] 当前 token 仍可用 /auth/me    -> 200 ✓
[6] 用旧密码登录                  -> 401 ✓
[7] 用新密码登录                  -> 200 ✓
```

### `/api/db/*` 校验

```
[8]  settings 非 JSON             -> 400 ✓
[9]  settings 传数组（应为对象）   -> 400 ✓
[10] vocab 传对象（应为数组）      -> 400 ✓
[11] settings 合法对象             -> 200 ✓
[12] GET settings                 -> 200 {"userTopic":"travel"} ✓
[13] 未知 key                      -> 400 ✓
```

### 请求体上限（chunked，不带 Content-Length）

```
chunked 9MB  -> /api/db/settings      -> 413 ✓
chunked 25MB -> /api/auth/login       -> 413 ✓
之后服务正常：1MB -> /api/db/vocab    -> 400（类型校验生效）✓
/api/health                            -> 200 ✓（进程未被拖垮）
```

### 备份同秒冲突

```
[backup#1] -> 200 20260828115534_chat.db
[backup#2] -> 200 20260828115534_134372r3k8_chat.db   ← 回退命名生效
[backup#3] -> 200 20260828115534_13437881ro_chat.db
```

（修复前 `backup#2` 为 500 `output file already exists`。）

### 未做的验证

以下需要真实浏览器 + MiniMax/ElevenLabs/Anki 环境，本轮未执行，建议你在实际使用中确认：

- 新账户首次进入并发送第一条消息
- 流式回复中点「停止」→ 无空气泡、上游确实停止
- 切换对话后旧评分不写入新对话
- A→B→A 账户切换无缓存串号
- 阅读高亮刷新后恢复
- 写作「开始写作」不再报错
- 题库单卷 `source_file` 有值、推送逐题结果正确
- Cloze / Markdown 代码块中含 HTML 时不执行

---

## 五、未修复的遗留项

除第三节三项外，以下问题本轮未处理：

### 后端

| 项 | 位置 | 说明 |
|---|---|---|
| Anki 代理 action 无白名单 | `server.js` `/api/proxy/anki` | 任意登录用户可传任意 AnkiConnect action，不限牌组归属。单用户场景风险有限 |
| session token 明文存储 | `sessions` 表 | 建议存 `SHA-256(token)`；另缺过期 session 清理、「退出所有设备」 |
| 无 schema 版本管理 | `server.js` 建表/`ALTER` | 靠 catch `ALTER TABLE` 异常判断列是否存在，会吞掉真实错误（只读、锁冲突、损坏）；建议 `PRAGMA user_version` |
| 无 graceful shutdown | 文件末尾 | 缺 SIGINT/SIGTERM handler、`db.close()`、WAL checkpoint |
| 无 `busy_timeout` | DB 初始化 | 与 `manage_users.py` 等外部工具并发写时可能 `SQLITE_BUSY` |
| 代理透明转发 | `/api/proxy/*` | 无模型白名单、无 token/字符预算、无成本上限；缺 key 时仍发空凭据请求 |
| CORS 允许 `Origin: null` | `ALLOWED_ORIGINS` | 不只匹配本项目 file://，任意本地 HTML/sandboxed iframe 均可 |
| 无安全响应头 | 静态响应 | 缺 CSP、`Referrer-Policy`、`frame-ancestors`（已加 nosniff） |
| Anki socket 手写 HTTP 解析 | `ankiCall()` | 逐 chunk `Buffer.concat`、无响应大小上限、不支持 chunked、按字符而非字节 slice |
| 备份仍由前端驱动 | — | 你已做部分改进（2 分钟 + 分层保留）；仍缺服务端常驻调度、备份校验、恢复工具、异盘副本 |

### 前端

| 项 | 位置 | 说明 |
|---|---|---|
| `app.js` 7900+ 行单体 | `js/app.js` | 全局可变状态跨领域共享，`wFeedback` 这类残留难以发现 |
| 大量 inline `onclick` | 全局 | HTML 转义 ≠ JS 字符串转义，存在转义脆弱点；也阻碍启用严格 CSP |
| 词典查询无请求身份校验 | 词典/划词 | 旧请求晚返回可覆盖新内容（标题是新词、正文是旧释义） |
| Anki 任务无互斥 | `processAnalysisForAnki` / `syncAnkiReviewData` / `maybeGenerateQuizQuestions` | 三者并发读写同一 weak 对象，可能重复出题或互相覆盖 `anki_notes` |
| 本地备份体积 | `localStorageBackup()` | 完整快照 ×N 份，易触达 localStorage 配额；建议改 IndexedDB 或仅存差异 |
| 可点击 `div`/`span` | 对话列表、题库、Cloze 候选等 | 键盘不可达、无 `aria-selected`/`aria-pressed` |
| 模态框无 focus trap | settings / topic / Anki review | 缺焦点圈定、Escape 关闭、背景 inert、关闭后焦点恢复 |
| 移动端 `100vh` | `css/style.css` | 软键盘/地址栏伸缩会遮挡输入区；建议 `100dvh` |
| 顶部导航横向滚动 | `index.html` header | 移动端控件过多，新对话可能被隐藏；音乐双击/滚轮在触屏不可用 |
| Wordle 无实体键盘 | Wordle 模块 | 仅屏幕键盘 |
| 搜索结果 prompt injection | `runResearch` | snippet 直接进 planner/summarizer prompt，未声明为不可信数据 |
| 无自动化测试 | — | 无 `package.json`、无测试入口、无 CI |

### 工程

- 无 `package.json`，Node 版本要求（22.5+，因 `node:sqlite`）只写在 README 注释里
- `start.bat` 不检查 Node 版本、端口占用、`/api/health` readiness，无论成功失败都提示「已启动」
- 自定义端口支持不完整（`ALLOWED_ORIGINS` 与 `start.bat` 硬编码 8091）
- 日志仅 console，无时间戳/request id/轮转；根目录与 `data/` 下有两套 `server.*.log`
- 遗留文件：`old/english-chat-index.html`、`__pycache__/server.cpython-313.pyc`、`data/*.json`（已迁移）、104 个旧 JSON 备份

---

## 六、体验改善建议

### 同步状态可见化

`apiSave` 现已维护 `syncStatus`（`pending` / `failed` / `lastError` / `lastSavedAt`），但尚未接 UI。建议在顶栏或侧栏显示：已保存 / 正在保存 / 离线待同步 N 项 / 保存失败可重试。这比现在「本地成功、服务端静默失败」更可信。

### 停止升级为任务中心

当前「停止」已能中止主回复 + 分析。可进一步显示阶段（策略师分析中 / 生成中 / 联网第 N 轮 / 汇总中 / 评分中 / Anki 推送中），支持停止单阶段、保留部分回复、重试失败阶段而非重跑全部。

### 联网来源前台展示

研究结果目前只在调试面板。建议在联网回复下方加可折叠来源卡：标题、域名、日期、链接、哪些结论引用了它；搜索失败或时效不足要明示。

### 自动草稿

作文 / 翻译 / Charade 按账户自动存草稿（500ms debounce），切模式不再频繁 confirm，可恢复最近版本。

### 阅读闭环增强

高亮已可恢复，可继续加：点击高亮编辑颜色/笔记、跳转段落、导出 Markdown、从高亮一键生成 Anki 卡、记住阅读位置、字号/行宽/夜间主题。

### 音频统一管理

建立 AudioManager 分 channel（聊天 TTS / 阅读 TTS / 背景音乐），语音播放时自动 duck 音乐，提供全局暂停，TTS 结果缓存避免重复计费。

### 版本树可视化

现在只显示「1/2 版本」。可加：版本来源（编辑/重新生成/停止后续写）、时间与摘要、分支命名、两版本对比、删除单分支。

---

## 七、新功能建议

按学习价值排序：

1. **学习者模型与仪表盘** — 把评分、薄弱点、词汇、Anki 复习整合成统一能力画像：CEFR 估计、四维度趋势、近 7/30 天练习量、高频错误及来源消息、Anki 覆盖率、周目标完成度。当前架构下价值最高。
2. **会话结束总结** — 每次对话结束生成「本次学到 3–5 个表达 + 最重要 1–3 个错误 + 建议复习」，用户确认后再进 Anki（避免自动推送稀释卡片质量）。
3. **完整口语训练闭环** — 浏览器录音 → 转写 → 发音/流利度/停顿/语调评分 → 原音对比 → 逐句影子跟读 → 同句多次录音进步曲线。比继续加文字小游戏更贴合「对话教练」定位。
4. **学习模式预设** — 把复杂开关包装成模式：快速聊天（仅主模型）/ 教学模式（+评分+薄弱点）/ 研究模式（+策略师+执行者+来源）/ 考试模式（限时、禁提示、统一评分）/ 隐私模式（不保存、不分析、不推 Anki）。同时降低调用成本。
5. **Anki 任务中心** — 持久化队列面板：待生成 / 待推送 / 已推送 / 重复跳过 / 失败，含目标牌组、note id、手动重试、预计 TTS 成本。
6. **成本与隐私中心** — MiniMax token、搜索次数、TTS 字符统计；日/月预算；超阈值自动降级；查看哪些消息发送给了哪些外部服务。
7. **个人数据导出/导入** — 结构化导出聊天、词汇、薄弱点、阅读笔记（区别于调试快照），支持版本迁移。

---

## 八、长期拓展方向

### 方向一：从「功能集合」到「个人英语学习系统」

目标数据链：

```
对话 / 写作 / 翻译 / 阅读
        ↓
统一错误与能力模型（learner model）
        ↓
个性化练习计划
        ↓
Anki + 网页练习
        ↓
复习结果回流
        ↓
动态调整下一轮内容
```

多数部件已有，缺的是统一 learner model 和可靠数据层。

### 方向二：Agent 编排从前端移到服务端

现在策略师 / 执行者 / 分析 / Anki 大量在前端串联，导致：切换对话易污染（本轮已加校验但属补丁）、关页面任务丢失、成本难统计。服务端任务化后：

```
message job → strategy → optional research → answer → evaluation → optional anki jobs
```

每阶段有状态、超时、取消、重试、成本记录；前端只订阅状态。

### 方向三：史官 / 观察者（需明确职责与预算）

- **史官**：压缩长期对话、维护稳定偏好与已学知识
- **观察者**：从多次表现识别趋势（而非单条消息）
- **教学规划器**：按目标与薄弱点安排下周练习

每个 agent 需固定输入、结构化输出、预算上限、用户开关。

### 方向四：数据与 provider 可迁移

统一 LLM provider 接口；搜索 provider 可替换；TTS 可回退浏览器 SpeechSynthesis；STT 本地/云可配；学习数据可导出导入与版本迁移。

### 方向五：数据模型正规化

`user_data.value` 大 JSON blob 适合原型，不适合并发。逐步拆为 `conversations` / `messages` / `vocabulary` / `weak_points` / `user_settings` / `gaokao_pushes` / `anki_jobs`，每行带 `user_id` + revision + 时间戳，实现细粒度事务、增量更新、冲突检测。

---

## 九、五批执行路线

### 第一批：安全止血

- [x] 静态目录穿越
- [x] Markdown 代码块 XSS
- [x] Cloze 模型输出 XSS
- [x] chunked 请求体上限
- [ ] Anki 卡片 HTML 净化（**用户决定跳过**）
- [ ] 默认弱口令账户（**用户决定跳过**）
- [ ] 登录限流 + PBKDF2 异步化（**用户决定跳过**）
- [ ] 收紧 `/api/proxy/anki` action 白名单与牌组归属
- [ ] session token 改存哈希 + 过期清理
- [ ] 补 CSP / Referrer-Policy / frame-ancestors

> 因穿越漏洞可读 `.env`/数据库/备份，建议轮换 MiniMax + ElevenLabs key，并清空 `sessions` 表要求重新登录。

### 第二批：数据可靠性

- [x] 跨账户 localStorage 隔离（owner 标记）
- [x] 保存串行队列 + 快照合并
- [x] `/api/db/*` JSON + 类型校验
- [x] 修改密码接口
- [x] 新账户初始化结构
- [x] 设置按账户 hydration + 快照同步
- [x] SSE 中止贯穿上游 + 三层超时 + 状态透传
- [x] 停止后空节点收尾
- [x] 评分任务归属校验 + 定时器管理
- [x] 高考推送逐题结果
- [ ] revision / ETag 乐观并发（服务端 CAS，冲突 409）
- [ ] `gaokao_pushed` 正规化为独立表
- [ ] Anki 任务队列互斥（按 weak point 加锁）
- [ ] 词典查询 request id 校验

### 第三批：质量基线

- [ ] 新增 `package.json`（`private`、`engines.node`、`start`、`test`）
- [ ] `node:test` 零依赖测试基线；`server.js` 拆出可注入 `DB_PATH` 的 app 工厂
- [ ] P0 测试：路径穿越、XSS corpus、A/B 账户隔离、保存乱序、chunked 413、修改密码、备份恢复、迁移原子性
- [ ] 前后端 API 契约测试（扫描前端 `/api/*` 调用是否都有路由）
- [ ] Playwright 六模块 + 三个 Game 子页签 smoke
- [ ] `PRAGMA user_version` + 事务化迁移
- [ ] `start.bat` 检查 Node 版本、端口占用、`/api/health` readiness

### 第四批：架构与体验

- [ ] 拆分 `app.js`：`js/core/`（api-client、auth-store、settings-store、sync-queue、task-manager、safe-render、dialog、audio-manager）+ `js/features/`（chat、reading、anki、writing、translation、gaokao、games）
- [ ] 后端拆 `server/`（config、auth、validation、rate-limit、migrations、routes、services）
- [ ] 取消 inline handler → `addEventListener` + `data-*`，随后启用严格 CSP
- [ ] 同步状态 UI（接 `syncStatus`）
- [ ] 移动端 `100dvh` + 底部导航 + overflow menu
- [ ] 模态框 focus trap / Escape / 焦点恢复；可点击元素改真实 button
- [ ] AudioManager 统一 TTS 与音乐
- [ ] 服务端常驻备份调度 + 备份校验 + 恢复 CLI + 异盘副本
- [ ] graceful shutdown + `busy_timeout` + 结构化日志与轮转

### 第五批：产品升级

- [ ] 学习者模型与仪表盘
- [ ] 会话结束总结（确认后进 Anki）
- [ ] 完整口语训练闭环
- [ ] 学习模式预设
- [ ] Anki 任务中心
- [ ] 阅读学习闭环增强
- [ ] 成本与隐私中心
- [ ] 服务端 Agent 编排
- [ ] `user_data` 正规化

---

## 近期目标

在继续开发新功能之前，建议先让系统满足这五条并能被自动验证：

1. **账户不串** — 任意账户切换顺序下都不会看到或回写他人数据
2. **数据不丢** — 并发/乱序保存不回退，损坏数据不能覆盖有效数据
3. **停止有效** — 停止后主回复、分析、上游调用、Anki 任务全部真正停止
4. **内容不执行脚本** — 所有不可信来源（模型输出、搜索结果、Anki 卡片）都不能注入可执行 HTML
5. **备份可恢复** — 不只是「生成了文件」，而是演练过能恢复

本轮已覆盖 1–3 和 4 的模型输出部分（Anki 卡片按你的决定跳过）；5 需要恢复工具与演练。
