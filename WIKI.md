# 🤖 AI 英语对话教练 · 操作指南

> 本文档覆盖所有已实现功能，按使用场景组织。

---

## 目录

1. [快速开始](#1-快速开始)
2. [登录与账户管理](#2-登录与账户管理)
3. [首页 + 6 大功能模块](#3-首页--6-大功能模块)
4. [Chat：对话基础](#4-chat对话基础)
5. [Reading：阅读模式（高亮 · 划词 · 背诵联动）](#5-reading阅读模式高亮--划词--背诵联动)
6. [Chat：角色系统](#6-chat角色系统)
7. [Practice：复习与卡片](#7-practice复习与卡片)
8. [Writing：作文与评分](#8-writing作文与评分)
9. [Translation：中译英练习](#9-translation中译英练习)
10. [Charade：猜词游戏](#10-charade猜词游戏)
11. [词典与翻译工具](#11-词典与翻译工具)
12. [划词翻译](#12-划词翻译)
13. [生词本](#13-生词本)
14. [薄弱点追踪](#14-薄弱点追踪)
15. [Anki 集成](#15-anki-集成)
16. [多 Agent 架构详解](#16-多-agent-架构详解)
17. [演讲朗读](#17-演讲朗读)
18. [斜杠命令](#18-斜杠命令)
19. [消息编辑与版本树](#19-消息编辑与版本树)
20. [设置面板详解](#20-设置面板详解)
21. [数据备份](#21-数据备份)
22. [调试面板](#22-调试面板)
23. [常见问题](#23-常见问题)

---

## 1. 快速开始

### 启动服务

```bash
cd ai-english-chat
node server.js
```

浏览器打开 [http://localhost:8091](http://localhost:8091)。

### 登录

默认账户：

| 用户名 | 密码 |
|---|---|
| `test` | `test` |
| `catten` | `catten` |

登录后数据按账户隔离存储，刷新页面自动恢复会话。

> 💡 **登录状态存储**：登录框默认勾选「记住我（保持登录状态）」→ token 存入 localStorage，关掉浏览器后 30 天内仍保持登录；取消勾选 → token 只存 sessionStorage，关闭标签页即退出。**公共电脑请不要勾选「记住我」。**

---

## 2. 登录与账户管理

### 登录界面

- 初始显示登录浮层
- 输入用户名和密码，点击「登录」或按 Enter
- 登录成功后自动加载该账户的历史数据

### 修改密码

在设置面板中：
1. 输入原密码
2. 输入新密码（至少 4 位，最多 200 位）
3. 点击「🔑 修改密码」
4. 成功后**其他会话（其他浏览器/设备）自动失效**，当前会话保持

> 修改密码由后端接口实现（`POST /api/auth/change-password`）。原密码错误会提示「原密码错误」。

### 退出登录

设置面板底部点击「🚪 退出登录」→ 确认后清除本地缓存并刷新页面。

### 用户管理脚本

```bash
python scripts/manage_users.py list              # 列出所有用户
python scripts/manage_users.py add <用户名> <密码>  # 添加用户
python scripts/manage_users.py reset <用户名> <新密码>  # 重置密码
python scripts/manage_users.py delete <用户名>    # 删除用户（级联清除数据）
```

---

## 3. 首页 + 6 大功能模块

### 首页

登录成功后自动进入首页。首页采用深色渐变背景（深蓝 → 深灰 → 深蓝，带径向光晕），居中显示 **6 张功能卡片**：

| 模块 | 图标 | 说明 |
|---|---|---|
| **CHAT** | 💬 | 与 AI 进行自由英语对话 |
| **READING** | 📖 | 阅读文章、划线高亮、划词翻译、联动背诵练习 |
| **PRACTICE** | 📝 | 复习薄弱点与 Anki 卡片 |
| **WRITING** | ✍️ | 写作文并获取 AI 详细评分 |
| **TRANSLATION** | 🌐 | 中译英练习（题库或 AI 出题） |
| **CHARADE** | 🎭 | 猜词描述游戏（题库或 AI 出词） |

点击任意卡片进入对应模块。也可点击顶部导航的「🏠 首页」按钮返回。

> 💡 **记住当前模式**：进入某个模块后，**刷新页面不会回到首页**，会自动恢复上次所在的模式（Game 模式还会记住子页签）。只有主动点击「🏠 首页」后刷新才回到首页。

### 顶部模式切换器

顶部导航的胶囊按钮可快速切换模式：

- 当前模式高亮为白底蓝字
- 鼠标悬停有半透明白色高亮
- 切换模式时保留左右两个侧栏（对话列表 + 反馈/词典）

### 各模式通用特点

- **左侧对话栏**：在 Chat 模式下显示历史对话；其他模式下显示 Anki 复习面板
- **右侧反馈栏**：在任何模式下都可访问「📊 反馈」和「🔤 词典翻译」标签
- **难度调节**：仅在 Chat 模式下显示（其他模式不需要）
- **新对话按钮**：仅在 Chat 模式下显示

---

## 4. Chat：对话基础

### 开始新对话

点击顶部「🔄 新对话」按钮，弹出**话题选择窗口**：

| 话题 | 说明 |
|---|---|
| 自由发挥 | 不限定话题 |
| 日常生活 | 日常生活与经历 |
| 科技 | 科技与数字生活 |
| 文化 | 美国文化、音乐、电影、艺术 |
| 旅行 | 旅行经历与计划 |
| 商业 | 商业与职业 |
| 环境 | 环境与可持续发展 |
| 健康 | 健康与保健 |
| 娱乐 | 娱乐与爱好 |
| 教育 | 教育与学习 |
| 雅思口语 P1 | 个人话题 |
| 雅思口语 P2 | 描述人物/地点/事物/经历 |
| 雅思口语 P3 | 抽象讨论与观点表达 |
| 雅思写作 Task1 | 图表/地图/流程描述 |
| 雅思写作 Task2 | 社会/环境/教育类议论文 |

选择话题后，AI 角色会自动根据话题开始对话。

### 发送消息

- 输入框输入英文，按 **Enter** 发送，**Shift+Enter** 换行
- 发送期间显示 `⏹ 停止` 按钮，可随时中止
- 输入 `/` 开头时弹出斜杠命令菜单

### 对话管理

左侧边栏：
- 列出所有对话，按更新时间排序
- 点击切换对话
- 鼠标悬停显示删除按钮
- 顶部的「＋ 新对话」按钮

### 难度调节

顶部滑块调节 CET 难度（1-10），控制 AI 使用的词汇和句式复杂度：

| 值 | 标签 |
|---|---|
| 1-2 | CET-初 |
| 3-4 | CET-4 |
| 5-6 | CET-6 |
| 7-8 | CET-6+ |
| 9-10 | CET-8+ |

---

## 5. Reading：阅读模式（高亮 · 划词 · 背诵联动）

Reading 模式让你像读真实文章一样浏览英文材料，同时把每一处不熟悉的词汇变成可点击、可翻译、可复习的学习单元。

### 入口

- 顶部模式胶囊 → **📖 Reading**
- 首页 → **READING** 卡片

### 选文章

进入模式后显示选择面板，提供三个来源：

| 标签 | 说明 |
|---|---|
| 📰 时政精选 | 8 篇内置文章（中英文混杂，覆盖联合国、AI、气候、慢阅读、家庭晚餐、翻译、工作方式、音乐回忆等话题） |
| 📝 粘贴文章 | 自己粘贴英文/中文文章，可选填标题 |
| 🕘 最近阅读 | 最近 30 篇阅读历史，一键恢复 |

点击任一卡片即可开始。文章以"报纸版式"卡片呈现：标题区（来源/语言/段数/时间）+ 段落正文（18-19px 中文 1.95 行高，约 70-90ch 阅读宽度）。

### 高亮笔

| 操作 | 效果 |
|---|---|
| 顶部 **🖍️ 高亮** 按钮 | 进入/退出高亮模式（鼠标变十字光标） |
| 高亮模式下选中文本 | 弹出 5 色选择器（黄/绿/蓝/粉/橙） |
| 点击高亮 | 显示关联笔记 |

5 色对应 `hl-yellow` / `hl-green` / `hl-blue` / `hl-pink` / `hl-orange` CSS 类。高亮、文章、笔记都按账户隔离保存到 `user_data.reading`。

### 划词翻译

在任何时候（非高亮模式）用鼠标划选一段文本：

- 单词 / 短语 → 弹窗显示音标、词性、变形、释义、例句、搭配、同义词辨析、词源
- 长句 → 句子级翻译 + 语法分析 + 关键词汇

弹窗有三个动作：
- **加入生词本**：写入全局生词本
- **📝 加笔记**：弹窗提示输入笔记，关联到当前文章
- **关闭**

### 阅读笔记面板

点击顶部 **📒 笔记** 打开右侧浮动笔记面板：

- 显示所有带笔记的高亮条目
- 每条笔记显示文本、笔记内容、颜色标识
- 可单个删除

### 朗读

点击 **🔊 朗读** 调用 ElevenLabs TTS 朗读全文（截断到 4000 字符），音频自动播放。

### 切换文章

点击 **📚 切换文章** 返回选择面板，最近阅读自动追加到历史。

### 一键背诵练习（联动主应用）

点击 **📝 背诵练习**：

- 打开新标签页到主应用 `article-memorizing/`（默认 `http://localhost:3000`）
- 通过 URL 参数 `?paste=<urlencoded>&lang=<zh|en>&title=<...>` 预填文章
- 主应用自动切换到「交互默写」tab，直接进入练习

URL 参数中如果需要自定义主应用端口，可使用 `?main=3001` 启动副应用。

> 主应用未运行时，打开的新标签页会显示"无法连接"。先启动主应用（`article-memorizing\server`）即可。

---

## 6. Chat：角色系统

### 内置角色

| 角色 | 国籍 | 城市 | 职业 | 特点 |
|---|---|---|---|---|
| **Alex Carter** 🇺🇸 | 美国 | 纽约布鲁克林 | 平面设计师 | 温暖风趣，爱旅行和独立音乐 |
| **Emma Thompson** 🇬🇧 | 英国 | 伦敦 | 记者/播客主 | 优雅有礼，热爱文学和历史 |
| **Sakura Tanaka** 🇯🇵 | 日本 | 东京 | 艺术史学生 | 友善创意，喜欢动漫和时尚 |
| **Mateo Rodriguez** 🇪🇸 | 西班牙 | 巴塞罗那 | 厨师/餐厅老板 | 热情开朗，热爱美食和足球 |

### 切换角色

在设置面板的「🎭 角色卡」区域点击角色卡片即可切换。

切换角色后，AI 会使用该角色的口吻、背景故事和个性进行对话，并自动生成新的背景故事。

### 自定义角色

设置面板中点击「＋ 新建角色」：

| 字段 | 说明 |
|---|---|
| 名字 | 角色名称 |
| 国籍/城市 | 如 American / Seattle |
| 职业/身份 | 如 high school teacher |
| 头像 Emoji | 如 🍀 |
| 性格/说话风格 | 详细描述角色的性格、兴趣、背景故事 |

创建后可在角色卡列表中切换，数据保存在服务端数据库。

### Chat 评分与反馈系统

#### 自动评分

每次发送消息后，系统自动调用 AI 对用户消息进行评分：

**四维度评分（每项 0-10）**：

| 维度 | 说明 |
|---|---|
| 语法 (grammar) | 语法正确性 |
| 表达 (expression) | 清晰度与含义传达 |
| 搭配 (collocation) | 自然词组搭配 |
| 风格 (style) | 文采与多样性 |

**反馈内容**：
- **语法纠错**：列出具体错误位置、修正版本、错误类型、规则解释
- **拓展知识**：同义词、习语、知识点、语法重点
- **新词推荐**：可添加到生词本的高频/实用词汇

#### 查看反馈

右侧面板「📊 反馈」标签页：
- 点击用户消息即可查看该消息的详细反馈
- 每条消息的反馈自动保存，切换对话后保留
- 反馈面板有三种模式：展开、小窗、收起

#### 重试分析

如果分析失败，消息上会显示「重试」按钮，点击重新请求分析。

#### 多 Agent 后台支持

Chat 模式下，每次回复背后会自动运行**策略师**和**执行者**两个后台 Agent，为 AI 回复提供风格/意图分析和联网搜索能力（详见 [第 15 节](#15-多-agent-架构详解)）。

---

## 12. 划词翻译

### 单次点击翻译

在 AI 回复气泡上**单击**任意英文单词：
- 自动选中该单词并打开翻译工具提示
- 显示词典释义（音标、词性、释义、例句、搭配、同义词辨析、词源）
- 流式输出，字段逐条渲染
- 支持词形变体自动归基（如 went → go）

### 拖选翻译

在 AI 回复气泡上**拖选**文本：
- 选中单词/短语/句子后自动弹出翻译工具提示
- **单词**：显示详细词典条目
- **短语**（2-4 个词，无句号）：短语级分析，含搭配说明
- **句子**（>4 个词或含句号）：句子翻译 + 语法结构分析 + 关键词汇
- 带中文的选中文本：自动识别为句子模式

### 翻译工具提示操作

| 操作 | 说明 |
|---|---|
| **拖动标题栏** | 移动提示位置 |
| **拖动右下角** | 调整提示大小 |
| **加入生词本** | 将当前词加入生词本 |
| **关闭** | 点击关闭按钮 |

### 右侧面板划词

在反馈面板和词典结果中同样支持划词翻译（不影响主聊天区的划词）。

---

## 7. Practice：复习与卡片

### 进入方式

从首页点击 **📝 PRACTICE** 卡片，或在 Chat 模式下从侧边栏点击「▶ 开始复习」。

### 显示内容

Practice 模式显示：
- Anki 连接状态（🗂️ 薄弱点牌组）
- 复习统计（待复习 / 新卡 / 学习中 / 总计）
- 今日复习数 + 连续学习天数
- 7 天柱状图
- 「✅ 开始网页复习」按钮 → 调用 `startWebReview()` 打开 Anki GUI 复习会话

### 完整复习流程

请参考 [第 14 节 Anki 集成 - 网页答题复习](#14-anki-集成) 了解复习、答题、同步等完整流程。

---

## 8. Writing：作文与评分

### 入口

首页点击 **✍️ WRITING** 卡片进入。

### 题目配置

顶部配置栏包含：

| 控件 | 说明 |
|---|---|
| **Exam Type** 下拉 | 高考英语 / 考研英语 / CET-4 / CET-6 / IELTS / TOEFL / GRE / 其他 |
| **Topic 文本框** | 多行 textarea，**支持换行**，可粘贴图片或拖拽图片 |
| **🖼️ 图片按钮** | 选择本地图片附加为题目（最大 5MB） |
| **预设题目按钮** | 点击即填入示例题目（6 个常用话题） |
| **🗑️ 清除** | 清空题目、图片和作文内容 |

### 题目展示区

选择或输入题目后，下方渐变背景的大字区域显示题目（保留换行），如果有附加图片，图片以缩略图形式展示，右上角 × 可删除。**支持拖拽图片到该区域**。

### 评分提交

写作区输入至少 20 个词后点击「📊 提交评分」：
- 评分包含：总分 + 优点 + 需改进 + 语法纠错 + 词汇建议 + 结构评价 + 示范句
- 实时字数统计（80 词以上显示 ✅ 标记）

### 多模态评分

如果附加了图片，提交评分时使用多模态内容（图片 + 文字）发送给 AI，AI 会同时参考图片和题目给出反馈。

### 内联标注反馈

提交评分后，除了右侧反馈面板的详细评分卡外，作文作答区会**直接覆盖一层内联标注视图**：
- 🟢 **正确**：绿色高亮
- 🔴 **错误**：红色高亮（语法/用词错误）
- 🟡 **可改进**：黄色高亮（表达不地道、可优化）

把鼠标悬停在标注片段上可查看 AI 给出的中文说明。点击右上角「✏️ 返回编辑」即可回到作答框继续修改。

### 作答框边缘留白

作文/翻译/描述作答框**默认在左右两侧留出较大边缘间距**（如同稿纸留白），随着内容变长会自动**缩短边缘留白**以容纳更多文字，避免文字始终贴着左右边缘。

---

## 9. Translation：中译英练习

### 入口

首页点击 **🌐 TRANSLATION** 卡片进入。

### 题目来源

顶部配置栏：

| 来源 | 说明 |
|---|---|
| 📚 题库 | 内置**上海高考 2020-2024 一二三模** 87 套试卷 348 道翻译题（带参考答案），按年份分类 |
| 🤖 AI 出题 | 调用 AI 生成中文句子 + 必用词（AI 出题也遵循当前翻译规则） |

### 🔑 必用词规则（高考版默认开启）

翻译题库的题目 JSON 中新增了 **「词」** 字段，代表该题**必须使用**的英文词：

**规则说明：**
- **必须用上**「词」里列出的所有词（翻译时必须用到）
- **词性不能变**（如果单词有多个词性，任选其一即可）
- 可使用**变式**：过去式、第三人称单数（-s/-es）、-ing、-ed、被动、名词复数、形容词副词等词性派生形式
- **首字母大写**的词：必须用在句首（例如 `It`、`Only`、`Out` 等）
- **高考版**额外要求：默认翻译为**完整的一句话**（尽量连分号也不要用）

**界面显示：**
- 题目卡片下方显示 **🔑 必用词** 胶囊标签（黄色 = 未使用，绿色 = 已使用）
- 提交后，AI 评分会明确列出 `missing_words`（未使用的必用词）和 `capital_issues`（大写位置错误）
- 本地也会快速检查，未使用/未变式的词会以红色「🔑 必用词（未使用）」标出
- 参考答案旁同样提示必用词

### 翻译规则版本（设置面板可选）

设置 → **🌐 翻译规则版本**，可选：

| 版本 | 说明 |
|---|---|
| 🏫 高考版（默认） | 必用词 + 单句输出，避免分号；用于高考真题题库自动启用 |
| 📚 标准版 | 必用词 + 可用 2-3 个分句/分号，追求自然 |
| 📜 旧版 | 不强制必用词，仅常规评分 |
| 🤖 自动 | 选择题库自动判定（上海高考 → 高考版，其他 → 标准版）；默认行为 |

> 默认（自动）模式下，只要当前题目来自「上海高考」题库就会自动采用高考版规则，无需手动切换。

### 操作流程

1. 点击「下一题 →」加载新题目（题库分类可用右侧题库面板选择）
2. 中文句显示在题目卡片（蓝色渐变背景）
3. 如有必用词，题目下方显示黄色/绿色胶囊
4. 在下方文本框输入英文翻译（**按回车直接提交**，Shift+回车可换行）
5. 点击「✅ 提交」或按回车 → AI 评分（0-10 分 + 改进建议 + 必用词达成度）
6. 评分结果：有参考答案的题会同时显示「📚 参考答案」+「🤖 AI译文」，无参考答案的题只显示「🤖 AI译文」
7. 或点击「显示参考答案」查看原文参考
8. 「下一题 →」继续下一题

### 内联标注反馈

提交翻译后，作答区同样会覆盖**内联标注视图**：🟢 正确（绿）、🔴 错误（红）、🟡 可改进（黄），悬停查看中文说明，点击「✏️ 返回编辑」回到作答框。

### 题库面板（右侧「📚 题库」标签页）

- 按年份分组展示全部试卷，可搜索年份/试卷名
- 展开试卷查看题目，每题显示题号、必用词、题目和参考答案
- 支持「📤 推送」/「📤 全部推送到 Anki」，卡片正面会附带「🔑 必用词」
- 已推送的题标记「✓ 已推 Anki」
- 也可切换为「🌐 翻译题库」模式，直接在右侧浏览/点选题目加载到主区

---

## 10. Charade：猜词游戏

### 入口

首页点击 **🎭 CHARADE** 卡片进入。

### 词汇来源

| 来源 | 说明 |
|---|---|
| 📚 题库 | 内置 10 个常用词（带英文提示） |
| 🤖 AI 出词 | AI 随机生成词汇和提示 |

### 游戏流程

1. 提示先显示（英文线索，例如「a figure dressed in old clothes, placed in a field to frighten birds」）
2. 点击「🎯 显示单词」查看要描述的目标词（如 **scarecrow**）
3. 在文本框用英文描述该词（不能直接说这个词本身）
4. 点击「✅ 提交评分」→ AI 评分（1-10 + 改进建议 + 参考描述）
5. 点击「下一个 →」继续

---

## 11. 词典与翻译工具

右侧面板「🔤 词典翻译」标签页：

### 输入与查询

输入框支持多种输入模式，自动识别：

| 模式 | 输入示例 | 输出 |
|---|---|---|
| **英词词典** | `apple` (≤3 个英文词) | 音标、词性、变形、释义、例句、搭配、同义词辨析、词源 |
| **中译英** | `我很期待` (含中文) | 多级别翻译（基础/自然/地道/高级），关键短语 |
| **句子分析** | `I enjoy reading books` (>3 个英文词) | 翻译、语法分析、词汇、短语 |
| **英语提问** | `/ask 过去完成时怎么用` | 详细解答，含词汇和知识点 |
| **普通提问** | `如何用英语表达加油` | 回答 + 拓展知识 |

### 快捷操作

- 在聊天输入框输入 `/t 文本` 或 `/translate 文本` 自动跳转到词典标签页并查询
- 在聊天输入框输入 `/ask 问题` 跳转到词典标签页并提问
- 查询结果中的短语和词汇可点击「+」快速加入生词本

### 流式渲染

词典查询结果采用**流式增量渲染**，字段逐个显示：
- 先显示 `type`、`word`、`phonetic`、`part` 等基本信息
- 再逐步显示 `translation`、`breakdown`、`examples` 等详细内容
- 底部显示原始 JSON 文本供参考

### 查询历史

词典面板顶部自动显示最近 12 条查询历史（按时间倒序）：
- 每条记录显示查询内容 + 时间戳（月-日 时:分）
- 点击历史记录可一键重新查询
- 重复查询会移到最顶部（去重）
- 顶部「🗑️ 清空」按钮可清除所有历史
- 历史记录按账户隔离存储在本地（localStorage），登录后保留

---

## 13. 生词本

### 添加方式

| 方式 | 操作 |
|---|---|
| 划词翻译 | 点击翻译工具提示的「加入生词本」 |
| 词典查询 | 点击查询结果中的「+」按钮 |
| 反馈面板 | 评分结果中的新词推荐可直接添加 |

### 管理生词

右侧面板「📖 生词本」区域：
- 卡片式列表，显示单词、释义、例句、日期
- 点击卡片查看详情（含原文语境）
- 每个卡片右上角「×」删除
- 底部「清空所有生词」按钮

### 数据存储

- 生词保存在服务端 SQLite 数据库（按账户隔离）
- 本地 localStorage 作为缓存
- 登录后自动从服务端加载

---

## 14. 薄弱点追踪

### 自动追踪

每次分析评分后，如果有语法纠错，系统自动将错误类型和内容加入薄弱点列表。

**薄弱点格式**：
- `category`：分类（语法搭配、词汇等）
- `point`：具体知识点
- `suggestion`：改进建议
- `count`：累计出错次数
- `interval`：当前复习间隔（天）
- `ease`：难度系数
- `streak`：连续掌握次数
- `last_quizzed`：上次复习时间

### 查看薄弱点

右侧面板「🎯 知识薄弱点」区域：
- 按出错次数降序排列
- 显示分类和知识点
- 点击「×」可删除单个薄弱点

### 薄弱点管理

数据格式 v2，旧格式（v1）自动迁移为新格式。词典查询结果中的薄弱点也会自动追踪。

---

## 15. Anki 集成

### 准备工作

1. 本地安装 Anki 桌面版
2. 安装 AnkiConnect 插件（插件代码：2055492159）
3. 默认端口 8765

### 功能开关

| 方式 | 操作 |
|---|---|
| 顶部按钮 | 点击「📚 Anki」按钮切换开关 |
| 设置面板 | 勾选「📚 自动添加到 Anki」 |
| 设置面板 | 细分开关：自动添加词汇 / 纠错 / 拓展 / 薄弱点出题 |

### 检测连接

设置面板中点击「🔄 检测连接」：
- 自动探测 AnkiConnect 可用地址（127.0.0.1:8765 或 localhost:8765）
- 缓存工作地址，后续请求直接使用
- 如连接失败，显示「重连」按钮

### 自动添加内容

开启后，每次分析评分自动添加四类卡片：

| 分类 | 来源 | 牌组 | 笔记类型 |
|---|---|---|---|
| 📖 词汇卡片 | 分析结果中的 `new_words` | `英语学习::<用户名>::词汇` | `英语学习-词汇`（默写） |
| 🔧 纠错卡片 | 分析结果中的 `corrections` | `英语学习::<用户名>::纠错` | Basic |
| 💡 拓展卡片 | 分析结果中的 `extensions` | `英语学习::<用户名>::拓展` | Basic |
| 🎯 薄弱点题目 | 薄弱点自动出题（见下文） | `英语学习::<用户名>::薄弱点` | `英语学习-薄弱点问答` |

### AI 自动出题（薄弱点 → 题目）

系统会根据薄弱点列表自动生成练习题，推送到 Anki：

**出题流程**：
1. 每次分析评分后，系统追踪薄弱点（`trackWeakPoints`）
2. 对未出够题目的薄弱点，调用 AI 生成题目
3. 每轮最多处理 6 个薄弱点，支持多轮调用
4. 题目类型：**选择题**（4 选 1）、**填空题**（带提示）、**纠错题**

**出题策略**：
- 支持一题覆盖多个薄弱点（多对多）
- 每薄弱点默认出 **2 道题**，可在设置中调整
- 生成的题目推送到 `英语学习::<用户名>::薄弱点` 牌组

**笔记类型**（自动创建）：
- 模型名：`英语学习-薄弱点问答`
- 字段：`Question` / `Answer` / `Explanation`
- 正反面模板：题目 → 答案 + 解析

### 网页答题复习（Web Review）

Anki 集成支持在浏览器中直接进行复习，无需切换到 Anki 桌面应用：

**开始复习**：
1. 连接 Anki（确保 Anki + AnkiConnect 已运行）
2. 对话记录侧边栏下方会显示 **Anki 复习面板**
3. 点击「▶ 开始复习」按钮
4. 自动打开 Anki GUI 复习会话，从薄弱点牌组取卡

**答题流程**：
1. 浏览器弹窗显示卡片正面（题目）
2. 点击「🤔 显示答案」查看背面
3. 自评四个等级（对应 Anki FSRS 排程）：

| 按钮 | 含义 | 排程效果 |
|---|---|---|
| 😰 忘记 | 完全没记住 | 重置间隔 |
| 🤔 模糊 | 想起来了但不确信 | 短间隔 |
| 😊 记得 | 正确回答且较确信 | 正常间隔 |
| 😎 简单 | 轻松回答 | 长间隔 |

4. 自动进入下一张卡片，直到复习完毕

**复习数据同步**：
- 复习后自动同步 `syncAnkiReviewData()`
- 从 Anki 拉取 card 的 interval / ease / reps / lapses 数据
- 更新到本地薄弱点存储（`wp.interval`, `wp.ease`, `wp.streak`）
- 如果 interval > 21 天且 reps >= 3，减少薄弱点 count
- 如果 count <= 0 且 interval > 21 天，自动归档薄弱点

### 复习统计面板

对话记录侧边栏显示 Anki 复习统计：

| 统计项 | 说明 |
|---|---|
| 🗂️ 总计 | 薄弱点牌组卡片总数 |
| 📝 待复习 | 需要复习的卡片数 |
| 🆕 新卡 | 尚未学习的新卡片 |
| 📖 学习中 | 正在学习的卡片 |
| 📊 今日复习 | 今日已复习卡片数 |
| 🔥 连续天数 | 连续学习天数 |
| 7 天柱状图 | 最近 7 天复习量趋势图 |

### 牌组结构

```
英语学习                              # 顶层牌组
└── <用户名>                          # 子牌组（多用户隔离）
    ├── 词汇                          # 生词卡片
    ├── 纠错                          # 语法纠错卡片
    ├── 拓展                          # 拓展知识卡片
    └── 薄弱点                        # AI 自动出题卡片
```

### 技术细节

- 所有 Anki 请求通过后端代理 `/api/proxy/anki` 转发（绕过浏览器 CORS 限制）
- 自动探测可用笔记类型，优先使用 Basic / Basic (and reversed card)
- 牌组不存在时自动创建
- 连接失败时显示 toast 提示
- 支持批量添加卡片（`addNotes` API），减少请求次数
- **addNote/addNotes 兜底**：AnkiConnect 部分版本的 `addNote` 不接受 `deckName` 参数（卡片落到默认牌组），前端在添加卡片后会自动调用 `changeDeck` 移动到正确牌组
- **词汇默写题型**：词汇卡使用专用模型 `英语学习-词汇`（Front=中文释义，Back=英文单词），模板美观（渐变背景、大字号、虚线分隔）

---

## 16. 多 Agent 架构详解

该应用采用**策略师 + 执行者**双 Agent 架构，所有后台运行记录仅显示在「调试」面板，主界面不展示。

### 15.1 策略师（Strategist）

**作用**：在 AI 回复前快速分析用户消息，提供风格/意图/话题建议。

**分析内容**：
- `style`：用户当前语气（如 playful、serious、shy）
- `predicted_intent`：用户意图（聊天/练习/求助/吐槽等）
- `topic_hooks`：2-3 个自然的话题跟进角度
- `tone_advice`：AI 应如何调整语调
- `question_suggestion`：推荐的跟进问题
- `vocab_suggestions`：建议融入的词汇（提升用户表达）
- `grammar_focus`：重点语法点
- `user_level_estimate`：预估 CEFR 级别
- `encouragement_note`：鼓励语
- `needs_search`：是否需要联网搜索
- `search_query`：如需搜索，搜索关键词

**触发方式**：
- 每次发送消息时自动触发（可在设置中关闭）
- 分析结果作为「策略师简报」注入到系统提示词中

### 15.2 执行者（Executor）

**作用**：需要实时信息时，执行多轮联网搜索研究。

**流程**：
1. **触发判定**：策略师指派或主对话模型输出 `[NEED_SEARCH]` 协议
2. **搜索**：调用 MiniMax 搜索 API 获取实时结果
3. **规划器**：判断资料是否足够，决定是否跟进第二轮搜索
4. **汇总**：将搜索结果整理成结构化摘要
5. **回答**：主对话模型基于研究结果生成回复

**支持场景**：新闻资讯、价格查询、最新事件、统计数据等需要实时信息的对话。

### 15.3 开关控制

在设置面板中可独立开关：
- 「🧠 策略师」：关闭后 AI 直接回复，不经过策略师分析
- 「🔎 执行者」：关闭后 AI 不联网，仅凭训练知识回答

---

## 17. 演讲朗读

### 手动朗读

每条 AI 回复下方有「🔊 朗读」按钮：
- 点击后调用 ElevenLabs TTS 生成语音
- 生成过程中显示「⏳ 生成中...」
- 播放中显示「🔊 播放中...」
- 播放完成后恢复「🔊 朗读」

### 自动朗读

顶部「🔊 Auto」按钮开启后，每次 AI 回复后自动播放语音（延迟 800ms）。

### 技术细节

- 通过后端代理 `/api/proxy/tts/:voice_id` 调用 ElevenLabs API
- 默认语音 ID：`BZgkqPqms7Kj9ulSkVzn`
- 默认模型：`eleven_v3`
- 语音设置：稳定性 0.5，相似度 0.75，语速 0.95

---

## 18. 斜杠命令

在聊天输入框输入 `/` 开头，弹出命令菜单：

| 命令 | 别名 | 说明 |
|---|---|---|
| `/new` | - | 开始新对话（选择话题） |
| `/topic` | - | 选择对话主题 |
| `/translate` | `/t` | 打开词典翻译并查询文本 |
| `/ask` | - | 向英语老师提问 |
| `/feedback` | - | 打开反馈面板 |
| `/compact` | - | 收起反馈面板 |
| `/settings` | - | 打开设置 |
| `/help` | - | 显示所有斜杠命令帮助 |

**使用示例**：
```
/t I'm looking forward to
/ask 如何区分"affect"和"effect"
```

---

## 19. 消息编辑与版本树

### 编辑消息

1. 点击用户消息的「✏️」编辑按钮
2. 在文本框中修改内容
3. 点击「✓ 保存」或「✕ 取消」

**保存后**：
- 截断该消息之后的所有对话
- 保留旧版本（点击「← →版本」切换）
- 自动重新发送并获取 AI 回复

### 版本切换

如果一条消息有多个版本，下方显示版本切换器：
- `←` 切到上一个版本
- `→` 切到下一个版本
- 显示 `当前版本号/总版本数`

### 删除消息

点击「🗑️」删除按钮：
- 确认后删除该消息及其后的所有回复
- 不可恢复

---

## 20. 设置面板详解

点击顶部「⚙️ 设置」按钮打开。

### 账户设置

| 功能 | 说明 |
|---|---|
| 头像上传 | 支持 JPG/PNG，最大 2MB，自动保存到服务器 |
| 当前账户 | 显示当前登录用户名 |
| 修改密码 | 输入原密码和新密码 |

### 角色卡

- 显示所有可用角色（内置 + 自定义）
- 点击切换角色
- 「＋ 新建角色」创建自定义角色

### 聊天偏好

| 开关 | 说明 |
|---|---|
| 📚 自动添加到 Anki | 每次分析后自动添加词汇和纠错卡片 |
| 🔊 自动朗读回复 | 每次 AI 回复后自动播放语音 |
| ⚡ 流式输出回复 | 开启后 AI 回复逐字显示（SSE） |
| 🧠 策略师 | 回复前分析风格与意图 |
| 🔎 执行者 | 需要时联网搜索 |

### 话题与回复长度

| 设置 | 说明 |
|---|---|
| 你想谈论的主题 | 引导 AI 偏向特定话题，留空不引导 |
| 回复长度 | 设置 AI 回复的长度限制（如「约 120 词」） |

### 作答设置（作文 / 翻译 / 描述作答框）

| 设置 | 说明 |
|---|---|
| 字体大小 | 作答框文本字号（13–24px，默认 15px） |
| 字体样式 | 默认（跟随系统）/ 无衬线 / 衬线 Serif / 等宽 Mono / 手写 Comic / 花体 Cursive |

- 作答框**默认左右留出较大边缘间距**，内容变长后自动缩短边缘留白以容纳更多文字
- 该设置同样作用于提交评分后的内联标注视图

### 策略师指令

**发送指令**：写入一条指令让策略师在每次回复前参考。

- **常驻**：勾选后写入系统提示词长期生效
- **不勾选**：只对下一条消息生效一次

**历史指令**：查看、删除已发送的指令。

### 连接状态

| 状态 | 说明 |
|---|---|
| 📚 AnkiConnect | 显示连接状态，可检测和重连 |
| 💾 数据存储 | 本地缓存状态 |
| 📡 后端服务器 | 后端在线状态 |
| 💾 数据备份 | 上次备份时间，可立即备份 |

---

## 21. 数据备份

### 自动备份

- 打开应用且登录后，前端每 **2 分钟**触发一次备份
- 本地备份：localStorage 快照（分层保留）
- 服务端备份：SQLite 数据库快照（`VACUUM INTO`）
- 备份文件：`data/backups/<时间戳>_chat.db`（同一秒并发时会追加毫秒+随机后缀，避免覆盖）
- 保留策略：服务端按 **2分钟 / 5分钟 / 10分钟 / 1小时 / 1天 / 2天 / 3天 / 7天 / 30天** 时间节点分层保留，每个节点保留最接近该时间的一份 + 最新一份

> ⚠️ 备份由**浏览器页面**触发（每 2 分钟的定时器），页面未打开时不会自动备份；需要服务端常驻定时备份时可配合计划任务调用 `POST /api/backup`。

### 手动备份

设置面板中点击「立即备份」：
- 本地 localStorage 快照
- 服务端数据库快照
- 显示 toast 提示结果

> 备份是 `VACUUM INTO` 生成的一致性快照，可直接用 SQLite 打开；恢复时需先停止服务再整体替换 `data/app.db`（注意 WAL/SHM 文件）。

---

## 22. 调试面板

点击顶部「🔧 调试」按钮打开。

### 后台 Agent 运行记录

- **策略师** 🧠：显示每次分析的结果（风格、意图、是否需要搜索）
- **执行者** 🔎：显示每轮搜索的查询词、结果数量、研究摘要
- 按时间排序，使用颜色区分 Agent 类型

### 消息级 Agent 元数据

- 每条用户消息的策略师分析和执行者研究结果
- 展开查看搜索轮次和摘要

### 调试快照

包含：
- 当前配置（难度、话题、模型）
- 最近对话摘要
- 最近 API 原始响应
- 调试日志
- 生词本和薄弱点快照
- 浏览器信息

点击「复制快照」可复制到剪贴板，用于问题排查。

---

## 24. 背景音乐

### 放置音乐

把音频文件（mp3 / m4a / ogg / wav / flac / opus / aac）直接放进 **`ai-english-chat/music/`** 目录，刷新页面后在顶部导航栏点击 🎵 按钮即可播放。

> 文件名为中文时建议正常命名（已支持 URL 编码），文件名前若有数字序号（如 `01 xxx.mp3`）会在气泡中自动去掉序号只显示曲名。

推荐下载来源（免费授权，适合学习背景乐）：

- **Pixabay Music**（pixabay.com/music）：海量免费纯音乐/氛围乐，无需署名，直接下载 mp3
- **Youtube 音频库 / YouTube 音乐**：登录 YouTube 创作者后台的「音频库」，大量免版权背景乐
- **耳聆网 / 爱给网**：中文环境音、白噪音、轻音乐素材站
- **网易云 / QQ 音乐**：下载自己的歌单到本地 mp3 后放入即可（个人使用）

适合的曲风：纯钢琴、Lo-Fi、轻古典（巴赫/肖邦/德彪西）、环境白噪音等低干扰背景乐。

### 操作方式

1. 顶部导航栏点击 **🎵** 按钮：**单击**播放/暂停
2. **双击**：切换下一首（循环播放，超出列表自动回到第一首）
3. 按钮上**滚动滚轮**：调节音量（5% 步进，右上角气泡显示当前音量）
4. **气泡提示**：播放时气泡显示当前曲名，2.5 秒后自动消失；暂停时显示「⏸ 已暂停」
5. 播放时按钮变为**绿色呼吸动画**，一目了然

### 设置面板

设置 →「🎵 背景音乐」提供独立控制：启用/停用顶部音乐按钮、播放结束自动下一首、选择曲目、音量以及上一首/播放暂停/下一首。

### 记忆与状态

- 音量、当前曲目索引保存在浏览器 localStorage（`ai_en_music_vol` / `ai_en_music_idx`）
- 刷新/重启后再次点击 🎵 会从上次的曲目继续
- 数据与账户无关（音乐文件为全局共享，个人偏好本地保存）

### 备份保留

浏览器本地快照和服务端 SQLite 快照每 2 分钟触发一次，但不会只保留最近一天；系统会分别保留最接近 **2分钟、5分钟、10分钟、1小时、1天、2天、3天、7天、30天** 的快照以及最新快照。

### 实现说明

- 后端 `server.js`：新增 `GET /api/music/list`（扫描 music 目录列出音频，无需鉴权）+ 静态白名单增加 `/music/*`（支持中文文件名 URL 解码）
- 前端 `index.html`：header 内新增 🎵 按钮 + 气泡元素；`app.js` 新增播放器逻辑（`musicInit`/`toggleMusic`/`musicPlayIdx`/`showMusicBubble` 等）

---

## 23. 常见问题

### Q: 对话没有回复/一直转圈？
A: 检查 `ai-english-chat/.env` 中是否正确配置了 `MINIMAX_API_KEY`，确认后端已启动且网络正常。

### Q: Anki 添加失败？
A: 确保 Anki 已打开且 AnkiConnect 插件已安装。点击设置中的「检测连接」查看状态。

### Q: 划词翻译没反应？
A: 确保点击的是 AI 回复气泡中的文本，而不是输入框或按钮区域。如果是单次点击，只能识别英文单词。

### Q: 语音朗读失败？
A: 检查是否配置了 `ELEVEN_API_KEY`，以及语音 ID 是否有效。

### Q: 刷新后对话不见了？
A: 登录后会自动从服务端加载数据。如果仍为空，检查是否登录了正确的账户。

### Q: 编辑消息后版本怎么切换？
A: 编辑后旧版本保留，在消息下方显示「← 1/2 版本 →」切换器。

### Q: 如何清除所有数据？
A: 设置中退出登录会清除本地缓存。如需清除服务端数据，需使用数据库管理工具操作 `data/app.db`。

### Q: 流式输出和普通输出有什么区别？
A: 流式输出（SSE）让 AI 回复逐字逐句显示，体验更流畅；普通输出需要等待完整回复生成后一次性显示。

### Q: 策略师指令和常驻指令的区别？
A: 普通指令只对下一条消息生效一次；常驻指令写入系统提示词，每轮对话都生效，直到手动删除。

### Q: 换账户登录后，看到的却是上一个账户的设置/历史？
A: 已修复。本地缓存现在带账户归属标记（`ai_en_cache_owner`），检测到账户不一致时会自动清空全部本地缓存再从服务端重新加载。退出登录也会清空 `ai_en_setting_*` 等全部用户态缓存。

### Q: 点了「⏹ 停止」，但后台好像还在跑？
A: 已修复。停止现在会同时中止：主回复（含上游 MiniMax 流）、评分分析、分析重试定时器。浏览器断开后服务端也会真正 abort 上游请求，不再继续产生调用。

### Q: 停止后聊天里出现空气泡 / `(no response)`？
A: 已修复。停止时若已有部分内容会保留并标记为已停止；完全没有内容则从版本树中移除该节点，不会再持久化空消息。

### Q: 阅读模式的高亮刷新后不见了？
A: 已修复。高亮现在保存「段落序号 + 段内字符偏移 + 原文」，重新渲染或切回历史文章时会按位置重建；若原文有变动导致无法精确匹配，该条高亮会被跳过而不影响其他高亮。

### Q: 保存的数据偶尔「回退」到旧版本？
A: 已修复。同一数据 key 的保存改为串行队列 + 只发送最新快照，避免旧请求后到覆盖新数据；服务端也会校验 JSON 合法性与顶层类型，非法数据不再能覆盖有效数据。
